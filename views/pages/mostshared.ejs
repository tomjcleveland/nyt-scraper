<%- include('../partials/header') %>
    <h2>Most shared articles</h2>
    <p>The top 20 most-shared NYT articles (on Facebook), right now.</p>
    <% articles.forEach((article) => { %>
      <%- include('../partials/articleCard', { article }) %>
    <% }); %>
<%- include('../partials/footer') %>
